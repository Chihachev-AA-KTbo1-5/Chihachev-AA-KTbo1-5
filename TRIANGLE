#include<stdio.h>
#include <math.h>
int main()
{

	int a, b, c;
	printf("Vvedite storoni trianle\n");
	scanf_s("%d %d %d", &a, &b, &c);
	if ((a + b > c) || (a + c > b) || (b + c > a))
	{
		printf("treungle is be\n");
		if ((a == b)&(b != c))
		{
			printf("triangle ravnibedriny");
		}
		if ((a == c)&(a != b))
		{
			printf("triangle ravnibedriny");
		}
		if ((b==c)&(a !=b))
		{
			printf("triangle ravnibedriny");
		}
		if ((a == b) && (a == c) && (b == c))
		{
			printf("triangle ravnostoronniy");

		}
		if ((a != b) && (a != c) && (b != c))
		{
			printf("triangle raznostoronniy");

		}





	}
	else
	{
		printf("triangle is not be");
	}





	return 0;
}
